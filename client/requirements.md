## Packages
framer-motion | Smooth animations and page transitions for chat bubbles

## Notes
Using the native browser Web Speech API (window.SpeechRecognition / window.webkitSpeechRecognition) for the microphone recording.
Assuming `@shared/routes` exports the `api` object and `@shared/schema` exports `Message`.
